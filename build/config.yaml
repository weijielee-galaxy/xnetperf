start_port: 20000
stream_type: "incast" # "fullmesh" or "incast" or "p2p", "localtest"
qp_num: 10
message_size_bytes: 4096
output_base: "./generated_scripts"
waiting_time_seconds: 5 # Time to wait before starting client scripts
speed: 400 # in Gbps, used for theoretical bandwidth calculation
rdma_cm: false
gid_index: 3 # GID index for RoCE v2, default is 3
network_interface : "bond0" # Network interface name for IP detection, default is "bond0"

report:
  enable: true
  dir: "/root"

run:
  infinitely: false
  duration_seconds: 20

ssh:
  private_key: ""

server:
  hostname:
  - "cetus-g88-094"
  hca:
  - "mlx5_0"
  - "mlx5_1"
client:
  hostname:
  - "cetus-g88-061"
  - "cetus-g88-062"
  hca:
  - "mlx5_0"
  - "mlx5_1"


version: "v1"